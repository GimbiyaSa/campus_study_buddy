// React components use JSX directly without explicit React import in newer versions
import PartnerMatching from '../components/PartnerMatching';

export default function Partners() {
  return <PartnerMatching />;
}
